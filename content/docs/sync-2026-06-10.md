# Sync 2026-06-10 — skills 구조 정리 (완료)

2026-06-10 skills 구조 정리(`_personal/` 해체 → `skills/` 직하 이동, 고아 파일 삭제)는 **완료됨**. 당시의 수동 동기화 절차와 "미처리 항목" 목록은 더 이상 유효하지 않다 — 그 항목들은 이후 cycle에서 해소됐다(예: hook matcher의 PowerShell 포함, settings.json의 `_template` 키 처리).

현재 모델: **dinner-harness repo가 source-of-truth**다. 두 머신/타깃 동기화는 수동 복사가 아니라 repo 편집 → `py -3 install.py --target claude|codex --allow-live`로 `~/.claude`/`~/.codex`를 재생성하는 방식이다. 상세는 repo `README.md` 참조.
