# Codex target

AGENTS.md is the concise native policy; the adapter installs it into the selected Codex home.
The default workflow keeps design, implementation and verification in the main session.
Independent native review and optional headless challenge/build are separate execution paths.

Model/effort/role permissions come from the logical routing profiles. Interactive model selection
is a recommendation until explicitly selected in the app/CLI; installation does not change an
open session. Common portable skill bodies and on-demand rules/roles/docs are installed without
requiring a Claude home. See the root README and dated CODEX-COVERAGE observations for evidence.

Edit repository source and validate a temporary target before any separately authorized live
installation. Preserve user-owned hooks/config/skills and historical capability measurements.
